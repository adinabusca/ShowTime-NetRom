{% extends 'base.html.twig' %}
{% block body %}
    <h1>Create User Details for {{ user.email }}</h1>
    {{ form_start(form) }}
    {{ form_row(form.firstName) }}
    {{ form_row(form.lastName) }}
    {{ form_row(form.age) }}
    {{ form_row(form.save) }}
    {{ form_end(form) }}
{% endblock %}
