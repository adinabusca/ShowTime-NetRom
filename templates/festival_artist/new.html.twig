{% extends 'base.html.twig' %}

{% block title %}Assign Artist to Festival{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>Assign Artist to Festival</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Submit</button>
        {{ form_end(form) }}

        <a href="{{ path('app_festival_artist') }}" class="btn btn-secondary mt-3">Back to List</a>
    </div>

{% endblock %}
